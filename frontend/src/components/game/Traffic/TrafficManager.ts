/**
 * @deprecated Use worldTraffic from ../World — kept for import compatibility.
 */
export { worldTraffic as trafficManager } from "../World/WorldTrafficEngine";
export type {
  AiPilotPhase as AiPhase,
  TrafficAircraft as AiAircraft,
} from "../World/types";
