/**
 * Cross-section profile generation lives in MeasurementTool
 * (shared click raycast). Re-export for modular imports.
 */
export { MeasurementTool as CrossSectionTool } from "../Measurement/MeasurementTool";
